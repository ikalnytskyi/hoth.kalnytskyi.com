name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install pre-commit
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade pre-commit

      - name: Run pre-commit
        run: |
          python -m pre_commit run --all-files --show-diff-on-failure

  site:
    runs-on: ubuntu-24.04

    services:
      webdav:
        image: bytemark/webdav
        env:
          USERNAME: test
          PASSWORD: test
        ports:
          - 8000:80

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install ansible
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade ansible

      - name: Install playbook dependencies
        run: |
          sudo apt install age skopeo umoci rsync

      - name: Create block storage device (volume)
        run: |
          VOLUME_DEVICE="$(losetup -f)"
          VOLUME_IMAGE="${{ runner.temp }}/diskimage"
          dd if=/dev/zero of=$VOLUME_IMAGE bs=1M count=1024
          sudo losetup $VOLUME_DEVICE $VOLUME_IMAGE
          sudo mkfs.ext4 $VOLUME_DEVICE
          echo "uri=$VOLUME_DEVICE" >> $GITHUB_OUTPUT
        id: volume-device

      - name: Create test backup age identity
        run: |
          age-keygen --output '${{ runner.temp }}/backup-age-key'
          echo "recipient=$(age-keygen -y '${{ runner.temp }}/backup-age-key')" >> "$GITHUB_OUTPUT"
        id: backup-age

      - name: Add server names to /etc/hosts
        run: |
          echo "127.0.0.1 hoth.kalnytskyi.local" | sudo tee -a /etc/hosts
          echo "127.0.0.1 vault.kalnytskyi.local" | sudo tee -a /etc/hosts
          echo "127.0.0.1 ntfy.kalnytskyi.local" | sudo tee -a /etc/hosts

      - name: Run the playbook
        run: |
          ansible-galaxy collection install -r requirements.yml
          ansible-playbook \
            -vvv \
            --inventory "hoth," \
            --connection local \
            --extra-vars _storage_device_mountpoint="$RUNNER_TEMP/storage" \
            --extra-vars backup_age_recipient='${{ steps.backup-age.outputs.recipient }}' \
            --extra-vars backup_webdav_username=test \
            --extra-vars backup_webdav_password=test \
            --extra-vars backup_webdav_url=http://127.0.0.1:8000/ \
            --extra-vars backup_webdav_vendor=other \
            --extra-vars storage_device='${{ steps.volume-device.outputs.uri }}' \
            --extra-vars vaultwarden_domain='vault.kalnytskyi.local' \
            --extra-vars vaultwarden_push_installation_id='id' \
            --extra-vars vaultwarden_push_installation_key='key' \
            --extra-vars vaultwarden_smtp_password='sw0rdf1sh' \
            --extra-vars vaultwarden_yubico_client_id='' \
            --extra-vars vaultwarden_yubico_secret_key='' \
            --extra-vars wireguard_private_key='qOanPI1Z6XlEcR+ebyPB7z5qr6F2S0FZP7iWe+4AqVA=' \
            --extra-vars ntfy_domain='ntfy.kalnytskyi.local' \
            --extra-vars ntfy_smtp_password='sw0rdf1sh' \
            --extra-vars ntfy_webpush_key_pub='pubkey' \
            --extra-vars ntfy_webpush_key='key' \
            site.yml
